# Terragrunt / Terraform
.terragrunt-cache/
.terragrunt-state/
.terraform/
*.tfstate
*.tfstate.*
crash.log
crash.*.log

# Keep provider lock files if you want reproducible runs; ignored by default here.
.terraform.lock.hcl

# Secrets — secret.hcl.example is the committed template
secret.hcl

# Local overrides
*.auto.tfvars
override.tf
override.tf.json
